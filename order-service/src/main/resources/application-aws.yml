server:
  port: 9092
spring:
  application:
   name: order-service
  datasource:
    url: jdbc:mysql://localhost:3306/e-order
    username: root
    password: root
  jpa:
    hibernate:
      ddlAuto: update

eureka:
  client:
    serviceUrl:
      "default": http://localhost:8761/eureka
